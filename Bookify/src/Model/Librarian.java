/*package Model;

import java.awt.print.Book;
import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;

import javax.swing.JOptionPane;
import javax.xml.bind.ParseConversionEvent;

public class Librarian extends Person{
	public int LibrarianID;
	Connection connection = null;
	*/
	
	/*
	 * public void update() {
	 * 
	 * try { sqliteConnection singelton= sqliteConnection.getInstance(); connection
	 * = singelton.dbConnector(); String query =
	 * "insert into Librarian (LibrarianID,UserName,Password,Country,FirstName,LastName,age) values (?,?,?,?,?,?,?)"
	 * ; PreparedStatement pst = connection.prepareStatement(query);
	 * 
	 * int i =1; pst.setInt(i++, LibrarianID); pst.setString(i++, Username);
	 * pst.setString(i++, Password); pst.setString(i++, Country); pst.setString(i++,
	 * FirstName); pst.setString(i++, LastName); pst.setInt(i++, Age);
	 * pst.execute(); pst.close(); } catch (Exception sqlException) {
	 * System.out.println(sqlException); } }
	 */

	
	
//}
